# مسار لوحة الإدارة

تبدأ لوحة الإدارة من المسار `/admin`. عند عدم وجود جلسة مدير، يعرض التطبيق نموذج الدخول المخصص. يرسل النموذج اسم المستخدم وكلمة المرور إلى الإجراء `admin.login` على الخادم، حيث تتم مقارنة القيم مع أسرار `ADMIN_USERNAME` و`ADMIN_PASSWORD`، ثم تُنشأ جلسة موقعة في كوكي HttpOnly ويُعاد تحميل الصفحة.

بعد نجاح الدخول، يقرأ `auth.me` جلسة المدير من الخادم ويعيد مستخدمًا بدور `admin`. عندها يمرّر المسار المحتوى إلى `DashboardLayout` ويعرض `AdminContent`. إجراءات `apps.adminList` و`apps.stats` وعمليات CRUD لا تقبل التنفيذ إلا من سياق يحمل دور `admin`.

لفتح نموذج الإضافة، يُستخدم زر **إضافة تطبيق** في أعلى اللوحة؛ هذا الزر يعيد تهيئة النموذج ويضبط حالة النافذة إلى مفتوحة. ولفتح نموذج التعديل، يُستخدم زر القلم في صف التطبيق؛ تُحوّل حقول JSON المخزنة إلى قوائم قابلة للتحرير ثم تُفتح النافذة نفسها. زر الحفظ يختار تلقائيًا بين `apps.create` و`apps.update` بحسب وجود تطبيق قيد التعديل.

يمكن تسجيل الخروج من زر **تسجيل الخروج** في رأس اللوحة، ويؤدي ذلك إلى حذف كوكي جلسة المدير وإعادة تحميل المسار إلى شاشة الدخول.
